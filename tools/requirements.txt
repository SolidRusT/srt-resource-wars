# Resource Wars - Python Dependencies
# Install with: pip install -r requirements.txt

# SVG Generation
svgwrite>=1.4.3

# SVG to PNG Conversion (multiple options, any one will work)
# Option 1: CairoSVG (best quality, but requires Cairo C library on Windows)
cairosvg>=2.7.0

# Option 2: svglib + reportlab (pure Python, good quality, recommended for Windows)
svglib>=1.5.1
reportlab>=4.0.0

# Image Processing
Pillow>=10.0.0

# Procedural Generation (for terrain)
noise>=1.2.2
